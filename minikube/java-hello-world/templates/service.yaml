apiVersion: v1
kind: Service
metadata:
  name: java-hello-world
spec:
  selector:
    app: java-hello-world
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: 8080
  type: {{ .Values.service.type }}