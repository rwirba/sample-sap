apiVersion: v1
kind: Service
metadata:
  name: java-hello-world
spec:
  type: {{ .Values.service.type }}
  externalIPs:
    - 54.237.216.232
  selector:
    app: java-hello-world
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: 8080