apiVersion: v1
kind: Service
metadata:
  name: java-hello-world
spec:
  type: {{ .Values.service.type }}
  externalIPs:
    - 54.91.68.172
  selector:
    app: java-hello-world
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: 8080