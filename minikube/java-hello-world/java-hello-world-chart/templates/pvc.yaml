apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "java-hello-world.fullname" . }}-pvc
spec:
  accessModes:
    - {{ .Values.persistence.accessMode }}
  resources:
    requests:
      storage: {{ .Values.persistence.size }}