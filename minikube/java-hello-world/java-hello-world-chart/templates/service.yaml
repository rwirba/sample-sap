apiVersion: v1
kind: Service
metadata:
  name: java-hello-world
spec:
  type: {{ .Values.service.type }}
  selector:
    app: java-hello-world
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: 8080