apiVersion: v1
kind: Service
metadata:
  name: vault-demo
spec:
  type: {{ .Values.service.type }}
  selector:
    app: vault-demo
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: 8200
