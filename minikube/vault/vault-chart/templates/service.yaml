apiVersion: v1
kind: Service
metadata:
  name: vault-demo
  namespace: demo
spec:
  type: {{ .Values.service.type }}
  selector:
    app: vault-demo
  ports:
    - name: http
      protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
