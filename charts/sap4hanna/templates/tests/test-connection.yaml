apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sap4hanna-chart.fullname" . }}-test-connection"
  labels:
    {{- include "sap4hanna-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sap4hanna-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
