apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "s4hanna.fullname" . }}-test-connection"
  labels:
    {{- include "s4hanna.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "s4hanna.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
