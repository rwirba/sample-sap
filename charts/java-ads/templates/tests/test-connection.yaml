apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "java-ads.fullname" . }}-test-connection"
  labels:
    {{- include "java-ads.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "java-ads.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
